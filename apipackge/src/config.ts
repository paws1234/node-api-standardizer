import { EnvelopeConfig } from './types';

export const defaultConfig: EnvelopeConfig = {
  keys: {
    success: 'success',
    data: 'data',
    error: 'error',
    meta: 'meta',
  },
  apiVersion: '1.0.0',
};
